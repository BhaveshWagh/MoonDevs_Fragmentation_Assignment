import React from 'react'

const useWallet = () => {
    // fetch your api to get the following data
  return (

        walletAddress,
        isWalletConnected,
        walletBalance,
        isBalanceError,
        openChainModal,
        walletChain,
        chains,
        openConnectModal,
      
  )
}

export default useWallet
